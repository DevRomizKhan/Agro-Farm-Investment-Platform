# Archived database migrations

The SQL files in this directory are historical reference material only. They
are not used by the Supabase CLI or application deployments.

The active schema and migration history live exclusively in
`supabase/migrations/`. New schema changes must be added there with a new
timestamped migration.
